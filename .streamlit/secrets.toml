# This file is required by Streamlit but not used when running in Snowflake
# The app uses the native Snowflake session connection automatically
# This file is included to prevent the "secrets.toml not found" error
[snowflake]
# Minimal configuration - connection uses Snowflake session when running in Snowflake
# These values are placeholders and won't be used in Snowflake environment
type = "snowflake"

